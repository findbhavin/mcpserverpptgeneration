version: '3.8'

services:
  pptx-generator:
    build:
      context: .
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
      - PPTX_OUTPUT_DIR=/app/outputs
      - BASE_URL=http://localhost:8000
    volumes:
      - shared-pptx-volume:/app/outputs

volumes:
  shared-pptx-volume:
